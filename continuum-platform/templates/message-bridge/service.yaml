{{- if .Values.messageBridge.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "continuum-platform.message-bridge.fullname" . }}
  labels:
    app.kubernetes.io/component: message-bridge
    {{- include "continuum-platform.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.messageBridge.service.port | default 8080 }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/component: message-bridge
    {{- include "continuum-platform.selectorLabels" . | nindent 4 }}
{{- end }}
